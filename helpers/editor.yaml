# This helper defines a window which opens vim and some git commands
# in the sessions default directory.
name: editor
cmd: nvim
panes:
  - cmd: git status
    split: vertical
    size: 20
  - cmd: git l
    split: horizontal
